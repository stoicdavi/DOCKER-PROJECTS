var radius = 7;
var diameter = process.env.diameter;
var area = Math.pow(radius, 2) * Math.PI;
var circumference = 2 * radius * Math.PI;
console.log(
    `The area of a circle with radius ${radius} cm 
    is ${area} square cm`


)
console.log(
    `The circumference of a circle with radius ${radius} cm
    is ${circumference} cm`
)
